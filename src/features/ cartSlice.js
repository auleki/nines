import { createAsyncThunk, createSlice } from '@reduxjs/toolkit'

const initialState = {
  cartItems: [],
  totalPrice: 0
}

export const cartSlice = {
  name: 'itemsInCart',
  initialState,
  reducers: {
    // yet to start
  }
}
